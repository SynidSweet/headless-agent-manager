# Production Environment
# Uses Caddy reverse proxy for both API and WebSocket

# Production uses window.location.origin via Caddy proxy
# These are fallback values for local production testing
VITE_API_URL=http://localhost:3000
VITE_WS_URL=http://localhost:3000
