# Development Environment - Alternate Ports
# This allows dev and prod to run simultaneously

VITE_PORT=5174
# API/WS URLs commented out to enable runtime detection
# When accessed via agents.dev.petter.ai, uses window.location.origin
# When accessed via localhost, uses http://localhost:3000
# VITE_API_URL=http://localhost:3001
# VITE_WS_URL=http://localhost:3001
